# MSR Data Service API
DATA_API_URL=http://localhost:7071

# Server transport: "stdio" (default for Claude Desktop) or "sse" (for HTTP clients)
MCP_TRANSPORT=stdio

# SSE server port (only used when MCP_TRANSPORT=sse)
MCP_PORT=3100
